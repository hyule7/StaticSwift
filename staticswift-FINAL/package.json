{
  "name": "staticswift-site",
  "version": "1.0.0",
  "description": "StaticSwift — Professional website design for UK small businesses",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^7.0.0",
    "exceljs": "^4.4.0",
    "googleapis": "^140.0.0",
    "nodemailer": "^6.9.0",
    "puppeteer": "^21.0.0",
    "stripe": "^14.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
